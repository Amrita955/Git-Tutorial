name: Issue Event Logger

on:
  issues:
    types: [opened, edited, closed, reopened]

jobs:
  log-issue-event:
    runs-on: ubuntu-latest
    steps:
      - name: Print Issue Event Details
        run: echo "📝 Issue Event: ${{ github.event.action }}"
