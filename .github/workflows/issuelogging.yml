name: Issue Event Logger
on:
  issues:
    types: [opened, edited, closed, reopened]
jobs:
  log-issue-event:
    runs-on: ubuntu-latest
    steps:
      - name: Print Issue Event Details
        env:
          ISSUE_ACTION: ${{ github.event.action }}
        run: echo "📝 Issue Event: $ISSUE_ACTION"
